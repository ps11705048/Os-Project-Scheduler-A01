#include<stdio.h>
#include<stdlib.h>
#include<unistd.h>
int main()
{
	int i,n;
	printf("\nEnter number of processes: ");
	scanf("%d",&n);
	int arrival[n];
	int burst[n],burst1[n];
	int waiting[n];
	int tarrival[n];
	int tq1=3;
	int tq2=6;
	int avgT=0;
	int avgW=0;
